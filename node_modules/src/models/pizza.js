export default class Pizza {
    constructor(nombre, libreDeGluten, Importe, descripcion, ) {
      this.nombre = nombre;
      this.libreDeGluten = libreDeGluten;
      this.Importe = Importe;
      this.descripcion = descripcion;
    }
  }